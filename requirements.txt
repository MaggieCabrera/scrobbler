# Core (Mac + Pi)
sounddevice       # BSD — audio capture
numpy             # BSD — audio data
shazamio          # MIT — track identification
pylast            # Apache 2.0 — Last.fm scrobbling
Pillow            # HPND — image rendering
python-dotenv     # BSD — loads .env
qrcode[pil]       # MIT — QR code generation for setup
